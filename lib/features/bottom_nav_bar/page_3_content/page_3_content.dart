import 'package:flutter/material.dart';


class Body3 extends StatelessWidget {
  const Body3({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange[200],
      child: Center(child: Text('Página 0'),),
    );
  }
}